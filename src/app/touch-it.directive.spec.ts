import { TouchItDirective } from './touch-it.directive';

describe('TouchItDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchItDirective();
    expect(directive).toBeTruthy();
  });
});
