import { InactiveMatchDirective } from './directives/inactive-match.directive';

describe('InactiveMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new InactiveMatchDirective();
    expect(directive).toBeTruthy();
  });
});
